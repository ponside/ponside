// Standalone operational scripts run exclusively in Node.js. This alias keeps
// Next.js's server-only boundary intact in application builds while allowing
// the shared server modules to be reused by the indexer CLI.
export {};
